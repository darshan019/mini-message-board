extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href="/new") Add new message 

  ul
    each msg in messages 
      li 
        h2 #{msg.user}:
        h3 #{msg.text}
        p Date added: #{msg.added}
        

